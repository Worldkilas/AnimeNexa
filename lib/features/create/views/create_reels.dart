import 'package:flutter/material.dart';

class CreateReels extends StatelessWidget {
  const CreateReels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
